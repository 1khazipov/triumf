import React from 'react';

interface TeachersProps {}

export const Teachers: React.FC<TeachersProps> = ({ }) => {
    return (
        <div>
            <h1>Фоточки преподавателей :)</h1>
        </div>
    );
};

export default Teachers;
