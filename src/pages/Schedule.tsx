import React from 'react';

interface ScheduleProps {}

export const Schedule: React.FC<ScheduleProps> = ({ }) => {
    return (
        <div>
            <h1>в 6 бегит в 8 пресс качат</h1>
        </div>
    );
};

export default Schedule;
