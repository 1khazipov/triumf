import React from 'react';

interface FeedbackProps {}

export const Feedback: React.FC<FeedbackProps> = ({ }) => {
    return (
        <div>
            <h1>спать нельзя а так норм</h1>
        </div>
    );
};

export default Feedback;
